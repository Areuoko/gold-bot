name: Run Gold Scraper

on:
  schedule:
    - cron: '0 */6 * * *' # اجرا هر 6 ساعت
  workflow_dispatch: # دکمه اجرا دستی

jobs:
  run-script:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'
          
      - name: Install Libraries
        run: pip install yfinance requests

      - name: Run Scraper
        env:
          # ⚠️ اینجا حتما آدرس ربات خودت رو چک کن که درست باشه
          WORKER_URL: 'https://gold-bot.tilapila007.workers.dev' 
          SECRET_KEY: ${{ secrets.SECRET_KEY }}
        # 👇 تغییر مهم اینجاست: آدرس فایل رو درست دادیم
        run: python scraper.py
